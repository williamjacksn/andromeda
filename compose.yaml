services:
  ntl-build:
    command:
      - build
    image: ghcr.io/williamjacksn/netlify-cli
    init: true
    volumes:
      - ./:/project

  ntl-shell:
    entrypoint:
      - /bin/sh
    image: ghcr.io/williamjacksn/netlify-cli
    init: true
    volumes:
      - ./:/project
